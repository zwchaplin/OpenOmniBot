# Python VLM Agent（`gui_agent.py`）`act` 方法算法设计说明

## 1. 目标与约束

基于你已完成的 `agent_base.py`（统一 I/O、模型调用、Prompt 构建、`act` 抽象方法），在 `gui_agent.py` 中实现可落地的 `act` 主流程：  
- 输入：用户任务指令 + 当前页面截图（可含历史上下文）  
- 输出：单步或多步操作指令序列（用于驱动 GUI 自动化执行）

设计目标：
1. 与 OpenOmniBot 的 VLM 主流程一致（工具调用协议、单步闭环、可恢复重试）。
2. 对模型输出做“解析-修复-校验”，提升稳定性。
3. 支持长期任务中的上下文压缩、重复链路抑制、人机交互接管。

---

## 2. 参考实现映射（OpenOmniBot）

可直接对齐的核心能力：
- **请求构建**：`VLMClient.buildUIOperationRequest`  
- **Prompt 分层**：`PromptTemplate.buildSystemPrompt / buildTurnUserPrompt / buildToolCallRetryPrompt`  
- **工具协议与参数约束**：`VLMToolDefinitions`  
- **参数修复与容错解析**：`VLMToolArgumentParser`  
- **动作执行层**：`ActionExecutor.executeAction`  
- **上下文更新**：`UIContextManager.updateContext / addKeyMemory`  
- **主循环编排**：`VLMOperationService.executeTask / executeSingleStep`  
- **上下文压缩纠错**：`CompactorAgent`  
- **重复链路检测**：`DetectDuplicateChain`

---

## 3. `act` 方法职责边界

`gui_agent.py::act()` 建议只做“**一步决策 + 一步执行闭环**”或“受控循环 N 步”：
1. 基于当前上下文构造本轮请求。
2. 调用 VLM 并获取工具调用结果。
3. 解析并校验 action。
4. 执行 action（或返回给外部执行器）。
5. 更新上下文并返回标准化结果。

不要把设备驱动细节、模型 SDK 细节、日志存储细节全部堆在 `act()` 内，应分层下沉。

---

## 4. 推荐实现架构（分层）

### L0：`GuiAgent.act()`（编排层）
- 控制重试次数、终止条件、回传结构。
- 串联以下子模块。

### L1：`RequestBuilder`（输入组装层）
- 组织 `system prompt`、`turn user prompt`、截图、多轮历史消息。
- 明确本轮只允许一个 tool call（协议约束）。

### L2：`ResponseParser`（解析层）
- 从模型响应中提取：`observation / thought / summary / tool_call`。
- 若无 tool_call：进入“协议纠偏重试”。

### L3：`ArgumentNormalizer`（修复校验层）
- 处理 JSON 包裹/截断/引号嵌套。
- 坐标别名归一（如 `point`, `[x,y]`, `start/end` -> `x/y/x1/y1/x2/y2`）。
- 基于工具 schema 做类型校验与必填校验。

### L4：`ActionExecutor`（执行层）
- 将结构化 action 映射为设备操作（click/type/scroll/back/home/wait...）。
- 统一产出 `OperationResult`。

### L5：`ContextManager`（状态层）
- 把本步 `UIStep` 写入 trace。
- 对 `record` 动作写入 key memory。
- 做重复链路检测和必要裁剪。

### L6：`Compactor`（长任务治理层，可选触发）
- 周期性压缩历史、输出 `running_summary/current_state/next_step_hint`。
- 在长链路时替换 trace，避免上下文膨胀。

---

## 5. `act` 主流程（建议）

1. **前置检查**：任务是否取消、是否暂停、步数是否超限。  
2. **采集输入**：最新截图 + 当前上下文（目标、历史总结、记忆、已完成里程碑）。  
3. **构造请求**：system + user + image + 最近 K 轮对话。  
4. **调用模型**：流式/非流式均可，优先保留 reasoning 文本用于可观测。  
5. **解析响应**：提取 tool_call 与元信息。  
6. **协议纠偏**（关键）：  
   - 无 tool_call 或多 tool_call -> 纠偏提示后重试（最多 M 次）。  
   - 参数非法 -> 先修复再校验，失败则进入重试。  
7. **执行动作**：返回或执行标准 action，记录结果。  
8. **更新上下文**：写 trace / key memory / steps_used。  
9. **终止判断**：  
   - `finished` -> 成功返回；  
   - `abort` -> 失败返回；  
   - 其他 -> 返回下一轮所需状态。  

---

## 6. 关键节点技术路线

1. **严格工具协议优先**  
   - 每轮仅允许 1 个 tool call。  
   - 统一 schema 定义（参数类型、必填、枚举值）。  

2. **“先修复后校验”解析策略**  
   - 先做 JSON 提取、松散格式修复、别名归一。  
   - 再做强校验（类型、坐标字段、必填）。  

3. **坐标处理与稳定性控制**  
   - 使用截图尺寸与设备尺寸做比例换算。  
   - 点击/滑动前后可做页面稳定性检测（XML 树或视觉差异）。  

4. **失败可恢复机制**  
   - 解析失败计数阈值。  
   - 网络/服务异常区分可重试与不可重试。  
   - tool 协议失败单独重试通道（与网络重试分离）。  

5. **长任务上下文治理**  
   - 固定步数或动态间隔触发 compactor。  
   - 将长 trace 转为 summary + state + next_hint，降低 token 压力。  

6. **人机协同动作**  
   - `info / require_user_choice / require_user_confirmation` 进入“等待用户输入”分支。  
   - 用户输入回写为 `record`，再继续自动执行。  

---

## 7. `gui_agent.py` 内部建议的最小模块划分

- `act()`：主编排  
- `_build_turn_request(context, screenshot, retry_state)`  
- `_call_vlm(request)`  
- `_parse_vlm_response(raw_response)`  
- `_normalize_and_validate_action(tool_name, args)`  
- `_execute_action(action)`（若你选择在 agent 内执行）  
- `_update_context(step)`  
- `_should_compact(step_index, context)` / `_compact_context(context, screenshot)`

以上划分可以直接对应 OpenOmniBot 的 Kotlin 设计，便于后续对齐能力与排障。

---

## 8. 分阶段落地建议

### Phase 1（MVP）
- 支持核心动作：`click/type/scroll/back/home/wait/finished`  
- 单轮 tool_call 协议 + 参数校验 + 基础重试  

### Phase 2（稳定性）
- 加入参数修复、坐标别名归一、协议纠偏重试  
- 加入解析失败分类与可观测日志

### Phase 3（长任务）
- 引入 compactor、重复链路抑制、人机接管动作  
- 增加 summary/记忆/里程碑治理

该路线可确保你先跑通，再逐步逼近项目级稳定性。
